{
  "name": "water-pit",
  "version": "0.0.2",
  "main": "lib/index.js",
  "scripts": {
    "pretest": "coffee --no-header -o ./ -c src/",
    "prepublish": "coffee --no-header -o ./ -c src/"
  },
  "dependencies": {
    "lodash": "^2.4.1"
  },
  "devDependencies": {
    "express": "^4.11.0"
  }
}
